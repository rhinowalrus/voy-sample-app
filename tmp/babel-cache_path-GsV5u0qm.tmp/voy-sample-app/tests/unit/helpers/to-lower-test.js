import { toLower } from '../../../helpers/to-lower';
import { module, test } from 'qunit';

module('Unit | Helper | to lower');

// Replace this with your real tests.
test('it works', function (assert) {
  var result = toLower(42);
  assert.ok(result);
});